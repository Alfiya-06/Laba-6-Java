# Лабараторная работа №6.

## Аптракова Альфия

## Группа ИТ-9,10

## Вариант 1

### 1. Класс Main

Основной класс проекта, демонстрирующий работу всех пользовательских аннотаций и их обработчиков.

Функции класса:

Создание тестовых объектов.

Вызов всех процессоров аннотаций.

Красивый вывод результатов в консоль.

Выполнение всех заданий по очереди.

Выполняемые задания:

Проверка аннотации `@Invoke`

Проверка аннотации `@Default`

Генерация строкового представления через `@ToString`

Вывод списка типов из аннотации` @Validate`

Чтение и вывод значений свойств аннотации `@Two`

Вывод кешируемых областей из аннотации `@Cache`

### 2. Аннотация `@Invoke`

Аннотация для автоматического вызова методов во время выполнения программы.

Характеристики:

Цель: METHOD

Не имеет свойств

Особенности:

Применяется только к методам без параметров

### 3. Класс `InvokeWorker`

Тестовый класс, содержащий методы для демонстрации работы @Invoke.

Содержит:

Методы, помеченные `@Invoke (job1, job3)`

Обычный метод без аннотации `(job2)`


### 4. Аннотация `@Default`

Аннотация для указания класса, используемого по умолчанию.

Характеристики:

Цель: TYPE, FIELD

Обязательное свойство: `Class<?> value()`

### 5. Класс DefaultProcessor

Обработчик аннотации `@Default`.

Основные методы:

`process(Object obj)` —
Определяет, присутствует ли аннотация, и выводит имя класса, указанного в `value()`.

Особенности:

Обрабатывает `null` с выводом сообщения об ошибке.

### 6. Аннотация `@ToString`

Аннотация, определяющая, нужно ли включать тип или поле в автоматически формируемое строковое представление объекта.

Характеристики:

Цель: TYPE, FIELD

Свойство:
`Mode value()` со значением по умолчанию: `Mode.YES`

Перечисление Mode:
`YES, NO`

Особенности:

Поля без аннотации включаются автоматически.

### 7. Класс ToStringGenerator

Генератор строкового представления объекта с учётом аннотации `@ToString`.

Отображает только поля:

аннотированные `@ToString(YES)`

или не аннотированные вообще

Исключает поля, помеченные0 `@ToString(NO)`

Возвращает строку "null" для null объекта

### 8. Класс ValidateProcessor

Обработчик аннотации `@Validate`.

Основной метод:

`process(Object obj)` —
выводит полные имена всех классов, указанных в свойстве `value()` аннотации.

Особенности:

Проверяет наличие аннотации

Обрабатывает `null` с сообщением об ошибке

### 9. Аннотация `@Two`

Аннотация с двумя обязательными свойствами.

Характеристики:

Цель: TYPE

Свойства:

`String first()`

`int second()`

### 10. Класс TwoProcessor

Обработчик пользовательской аннотации `@Two`.

Основной метод:

`process(Object obj)` —
читает аннотацию и выводит значения свойств `first` и `second`.

Особенности:

Проверяет наличие аннотации на классе

Обрабатывает null входные данные

### 11. Аннотация `@Cache`

Аннотация для указания кешируемых областей класса.

Характеристики:

Цель: TYPE
Свойство: `String[] value()` со значением по умолчанию: пустой массив

### 12. Класс `DataProcessor`

Тестовый класс, аннотированный `@Cache({"users", "sessions", "config"})`.

Особенности:

Пустой класс, служит для демонстрации кешируемых областей

### 13. Класс `CacheProcessor`

Обработчик аннотации @Cache.

Основной метод:

`process(Class<?> clazz)` — выводит список всех кешируемых областей или сообщение, что список пуст.
Особенности:

Проверяет наличие аннотации

Обрабатывает null с сообщением об ошибке

Выводит читаемый список областей с кавычками
